/* ============================================================
   ShipX Billing — configuration (matches what's deployed)
   Supabase project: shipx-billing
   The publishable key is safe to expose in the browser because
   Row-Level Security (supabase-schema.sql) blocks anyone not logged in.
   ============================================================ */
window.SHIPX_CONFIG = {
  SUPABASE_URL:      "https://zvrxsbqggovvxtbisibm.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_72z_KVUE8suV2lbIFXzCVg_t37x4md8"
};
